

#instalar nginx
sudo apt update
sudo apt install nginx

cd /var/www/html
sudo vim prueba.html
presionamos la tecla i
escribimos <h1>hola mundo aws</h1>
presionamos esc + :wq


#instalar nvm
sudo apt install curl
curl https://raw.githubusercontent.com/creationix/nvm/master/install.sh | bash
source ~/.profile